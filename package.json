{
  "name": "dum-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.16.3",
    "prop-types": "^15.6.2",
    "react": "^16.5.2",
    "react-async-component": "^2.0.0",
    "react-dom": "^16.5.2",
    "react-router": "^4.3.1",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.5"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "serve": "^10.0.1"
  },
  "engines": {
    "node": "8.11.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server": "node --experimental-modules src/server.mjs",
    "server-dev": "nodemon --experimental-modules src/server.mjs",
    "dev": "concurrently --kill-others-on-fail \"yarn server-dev\" \"yarn start\"",
    "deploy": "node --experimental-modules src/server.mjs"
  }
}
